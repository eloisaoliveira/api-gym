version: '3'

services:
  api-solid-pg:
    image: bitnami/postgresql
    ports:
      - 5432:5432
    environment:
      - POSTGRESQL_USERNAME=eloisa
      - POSTGRESQL_PASSWORD=jujuca
      - POSTGRESQL_DATABASE=apisolid